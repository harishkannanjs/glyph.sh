---
import { siteConfig } from '@/site.config';

interface Props {
  postTitle?: string;
  postSlug?: string;
}

const { postTitle } = Astro.props;

const giscusConfig = siteConfig.giscus;
const isConfigured = Boolean(
  giscusConfig?.enabled &&
  giscusConfig?.repo &&
  giscusConfig?.repoId &&
  giscusConfig?.categoryId
);

// Derive clean GitHub repository and discussions link
const repoUrl = siteConfig.githubUrl ? `${siteConfig.githubUrl}/blog` : `https://github.com/harishkannanjs/blog`;
const discussionsUrl = `${repoUrl}/discussions`;
const topicDiscussionUrl = postTitle
  ? `${discussionsUrl}/new?category=general&title=${encodeURIComponent(`[Discussion] ${postTitle}`)}`
  : discussionsUrl;
---

<section class="mt-12 pt-8 border-t border-outline-variant font-mono">
  <div class="flex items-center justify-between pb-4 mb-4 border-b border-outline-variant/60 text-[12px] text-outline">
    <div class="flex items-center gap-2 uppercase tracking-wider">
      <span class="text-primary font-bold">&gt;</span>
      <span>Discussions &amp; Peer Review</span>
    </div>
    <span class="text-on-surface-variant text-[11px]">hosted in github repository</span>
  </div>

  {isConfigured ? (
    <div id="giscus-container" class="min-h-[160px]">
      <!-- Giscus script injected client-side with proper theme -->
    </div>
  ) : (
    <div class="p-4 bg-surface-container-low border border-outline-variant rounded-[2px] text-[12px] space-y-3">
      <div class="flex items-center justify-between">
        <div class="flex items-center gap-2 text-primary font-semibold">
          <span class="font-bold">&gt;</span>
          <span class="text-on-surface">Community Discussions (GitHub Discussions)</span>
        </div>
        <span class="text-[10px] text-primary border border-primary/30 px-1.5 py-0.5 rounded-[2px]">zero-risk</span>
      </div>

      <p class="text-on-surface-variant leading-relaxed text-[12px]">
        Comments and review discussions are stored safely inside your GitHub repository under the <strong>Discussions</strong> tab.
        Visitors authenticate with their own GitHub accounts, giving you spam protection, markdown code formatting, and moderation without granting any write access to your codebase.
      </p>

      <div class="flex flex-wrap items-center gap-3 pt-2">
        <a
          href={topicDiscussionUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="px-3 py-1.5 bg-surface-container border border-outline-variant hover:border-primary text-primary hover:underline text-[11px] rounded-[2px] inline-flex items-center gap-1.5 transition-colors cursor-pointer"
        >
          <span>Open GitHub Discussions</span>
          <span>↗</span>
        </a>
      </div>

      <div class="pt-2 border-t border-outline-variant/60 text-[11px] text-outline leading-relaxed">
        <span># To embed comments directly on this page: enable Discussions in your GitHub repository settings, install the free <a href="https://giscus.app" target="_blank" rel="noopener noreferrer" class="text-primary hover:underline">Giscus GitHub App</a>, and paste your repoId and categoryId into <code class="text-primary">profile.json</code>.</span>
      </div>
    </div>
  )}
</section>

{isConfigured && (
  <script is:inline define:vars={{ giscusConfig }}>
    function loadGiscus() {
      const container = document.getElementById('giscus-container');
      if (!container) return;

      container.innerHTML = '';
      const isDark = document.documentElement.classList.contains('dark');
      const theme = isDark ? 'transparent_dark' : 'light';

      const script = document.createElement('script');
      script.src = 'https://giscus.app/client.js';
      script.setAttribute('data-repo', giscusConfig.repo);
      script.setAttribute('data-repo-id', giscusConfig.repoId);
      script.setAttribute('data-category', giscusConfig.category || 'General');
      script.setAttribute('data-category-id', giscusConfig.categoryId);
      script.setAttribute('data-mapping', 'pathname');
      script.setAttribute('data-strict', '0');
      script.setAttribute('data-reactions-enabled', '1');
      script.setAttribute('data-emit-metadata', '0');
      script.setAttribute('data-input-position', 'bottom');
      script.setAttribute('data-theme', theme);
      script.setAttribute('data-lang', 'en');
      script.crossOrigin = 'anonymous';
      script.async = true;

      container.appendChild(script);
    }

    function handleThemeChange(e) {
      const nextTheme = e.detail?.theme === 'light' ? 'light' : 'transparent_dark';
      const iframe = document.querySelector('iframe.giscus-frame');
      if (iframe && iframe.contentWindow) {
        iframe.contentWindow.postMessage(
          { giscus: { setConfig: { theme: nextTheme } } },
          'https://giscus.app'
        );
      }
    }

    loadGiscus();
    window.addEventListener('theme-change', handleThemeChange);
    document.addEventListener('astro:page-load', loadGiscus);
  </script>
)}
